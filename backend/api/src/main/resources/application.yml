# backend/src/main/resources/application.yml
spring:
  datasource:
    url: ${DB_URL:jdbc:mysql://mysql-25f2a1ac-paulinogoncalves2022-91ae.d.aivencloud.com:17230/defaultdb}
    username: ${DB_USERNAME:avnadmin}
    password: ${DB_PASSWORD:AVNS_SBAl6oJLFSUXxd5sVz3}
    driver-class-name: com.mysql.cj.jdbc.Driver
    hikari:
      maximum-pool-size: 5
      connection-timeout: 30000
      idle-timeout: 600000
      max-lifetime: 1800000

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
        format_sql: false

  flyway:
    enabled: false  # ← desactivado até teres scripts em db/migration

bit:
  ai:
    openai-key: ${OPENAI_API_KEY:placeholder}
  jwt:
    secret: ${JWT_SECRET:local-dev-secret-key-placeholder}

server:
  port: 8080